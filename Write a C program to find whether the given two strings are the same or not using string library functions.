
#include<iostream>
#include<string>
using namespace std;
int main() {
    string a, b;

    cin >> a;
    cin >> b;
    
    if (a == b) {
        cout << "Strings are same" << endl;
    } else {
        cout << "Strings are not same" << endl;
    }

    return 0;
}
