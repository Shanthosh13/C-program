#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;

    int term = 0;
    int diff = 3;

    for (int i = 1; i <= n; i++) {
        cout << term << " ";
        term += diff;
        diff += 2;
    }

    return 0;
}
