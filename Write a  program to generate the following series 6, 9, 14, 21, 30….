#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;

    int term = 6;
    int diff = 3;

    for (int i = 1; i <= n; i++) {
        cout << term << " ";
        term = term + diff;
        diff += 2; // Increase the difference by 2 each time
    }

    return 0;
}
