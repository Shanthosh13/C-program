#include <iostream>
#include <iomanip>
using namespace std;

int main() {
    int x1, y1, x2, y2;
    cin >> x1 >> y1 >> x2 >> y2;

    double x = (x1 + x2) / 2.0;
    double y = (y1 + y2) / 2.0;

    cout<<fixed<<setprecision(1)<<x<<endl<<y<<endl;

    return 0;
}
