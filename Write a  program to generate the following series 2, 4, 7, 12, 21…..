#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;

    int term = 2;
    int diff = 2;
    int inc = 1;

    for (int i = 1; i <= n; i++) {
        cout << term << " ";
        term += diff;
        diff += inc;
        inc *= 2;
    }

    return 0;
}
