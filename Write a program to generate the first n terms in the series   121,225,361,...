#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;

    int root = 11;

    for (int i = 1; i <= n; i++) {
        cout << root * root << " ";
        root += 4;
    }

    return 0;
}
