#include <iostream>
using namespace std;

int main() {
    int num, limit;
    cin >> num >> limit;

    for (int i = 1; i <= limit; ++i) {
        cout << num << " * " << i << " = " << num * i << endl;
    }

    return 0;
}
